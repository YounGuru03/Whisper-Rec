# Requirements for building Windows executable
# This includes the main dependencies plus PyInstaller
pyinstaller>=6.0.0
pillow  # For potential icon support
auto-py-to-exe  # Optional GUI for PyInstaller (not used in automation)

# Core dependencies (from pyproject.toml)
fastapi
librosa
soundfile
faster-whisper
uvicorn
websockets
torchaudio>=2.0.0
torch>=2.0.0
tqdm
tiktoken
triton>=2.0.0; platform_machine == "x86_64" and (sys_platform == "linux" or sys_platform == "linux2")

# Additional dependencies for Windows build stability
cffi  # Ensure C foreign function interface is available
setuptools  # Sometimes needed for PyInstaller
wheel  # Build tool